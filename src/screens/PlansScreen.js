import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PlansScreen = ({ navigation }) => {
  return (
    <View>
      <Text>Estos son los planes, tetes</Text>
    </View>
  );
};

export default PlansScreen;

const styles = StyleSheet.create({});
